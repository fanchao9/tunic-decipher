export interface Translation {
  id: string;
  x: number;
  y: number;
  tunicText: string;
  englishText: string;
}

export interface ManualPage {
  id: string;
  pageNumber: number;
  imageUrl: string;
  title: string;
}
